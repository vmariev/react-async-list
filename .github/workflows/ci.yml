name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # jsdom 30 requires ^22.22.2 || ^24.15.0, so Node 20 cannot run the
        # tests at all — and it reached end of life in April 2026 regardless.
        node-version: [22, 24]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - run: npm ci

      - run: npm run lint

      - run: npm run format:check

      - run: npm run typecheck

      - run: npm test

      - run: npm run build

      # The generated stylesheet module is committed; fail if it drifted from
      # the authored CSS (the build above regenerates it).
      - run: git diff --exit-code -- src/styles/generated.ts

      - run: npm pack --dry-run
