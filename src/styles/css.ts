export { ASYNC_LIST_CSS } from './generated';

/**
 * `id` of the `<style>` element injected by `useInjectedStyles`. Exported so you
 * can detect or replace the stylesheet.
 */
export const STYLE_ELEMENT_ID = 'react-async-list-styles';
